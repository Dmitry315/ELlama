from setuptools import setup, find_packages

setup(
    name="convert",
    version="0.1.0",
    packages=find_packages()
)

setup(
    name="data_process",
    version="0.1.0",
    packages=find_packages()
)

setup(
    name="inference",
    version="0.1.0",
    packages=find_packages()
)